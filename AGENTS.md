# AGENTS.md

## Cursor Cloud specific instructions

This is a blank GitLab scaffold repository (`gitlab.com/davos/rutasec`). As of the initial setup, the repo contains only the default GitLab `README.md` template and no application code, dependencies, or services.

- **No build/lint/test commands exist yet.** Future agents should re-evaluate once application code is added.
- **No services to start.** There are no backend, frontend, or database services defined.
- **No dependency files present** (`package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, etc.).

When application code is added to this repository, update this file with relevant build, test, and run instructions.
